---
title: UIDropdown
group:
  title: 通用
---

# UIDropdown

:::info

为了 更好的服务业务逻辑，支持了 footer 属性，可以做到 footer 区域自动沉底。 需要配合 useCustomStyle 开启。

```
footer?: React.ReactNode;
width?: number | string;
maxHeight?: number | string;
useCustomStyle?: boolean;
```

进行使用

:::

UIDropdown

<code src="./example/demo2.tsx"></code>
